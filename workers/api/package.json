{
  "name": "hf-vault-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute hf-vault --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute hf-vault --local --file=./schema.sql"
  },
  "dependencies": {
    "@huggingface/hub": "^2.13.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
